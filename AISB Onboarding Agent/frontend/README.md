# Frontend - Bootcamp Onboarding

## Setup

```bash
cd frontend
pnpm install # or npm install / yarn
```

Create `.env.local`:

```
NEXT_PUBLIC_API_BASE=http://localhost:8000
```

## Run

```bash
pnpm dev # or npm run dev
```
